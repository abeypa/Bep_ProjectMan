{
  "name": "taskflow-monorepo",
  "private": true,
  "scripts": {
    "build": "cd taskflow/frontend && npm install && npm run build",
    "deploy": "npx wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.84.1"
  }
}
