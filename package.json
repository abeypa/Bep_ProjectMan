{
  "name": "taskflow-monorepo",
  "private": true,
  "scripts": {
    "build": "cd taskflow/frontend && npm install && npm run build",
    "deploy": "npx wrangler pages deploy taskflow/frontend/dist --project-name bepprojectmanag"
  },
  "devDependencies": {
    "wrangler": "^3.84.1"
  }
}
